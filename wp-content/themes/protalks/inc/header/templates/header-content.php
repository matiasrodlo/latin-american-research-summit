<?php

if ( ! defined( 'ABSPATH' ) ) {
	// Exit if accessed directly.
	exit;
}

// Include logo.
protalks_template_part( 'header', 'templates/parts/logo' );

// Include main navigation.
protalks_template_part( 'header', 'templates/parts/navigation' );
